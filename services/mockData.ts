// Mock data has been removed to rely on the database.
export const GAME_TIERS = [];
export const CURRENT_USER = null;
export const BOT_PROFILE = null;
export const MOCK_TABLES = [];
export const MOCK_PLAYERS = [];
export const MOCK_TRANSACTIONS = [];
export const generateHash = (seed: string) => "";
